# Development environment install. pyproject.toml is the authoritative
# dependency specification — this file just installs the package editable
# with the optional extras a dev machine needs.
-e .[cine,dev]
